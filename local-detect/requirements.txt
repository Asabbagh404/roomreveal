# RoomReveal local detection service (Grounding DINO + SAM).
# For GPU, install a CUDA build of torch first, e.g.:
#   pip install torch --index-url https://download.pytorch.org/whl/cu121
fastapi
uvicorn[standard]
python-multipart
torch
transformers>=4.40
pillow
numpy
accelerate
# Motion Brush backend (Story 4.8): imageio + a static ffmpeg binary (pip only,
# no system dependency) for the /reverse route's time-reversal.
imageio[ffmpeg]
